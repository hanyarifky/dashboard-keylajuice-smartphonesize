{
  "name": "dashboard-keylajuice-smartphonesize",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx tailwindcss -i ./input.css -o ./src/style.css",
    "build-watch": "npx tailwindcss -i ./input.css -o ./src/style.css --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "tailwindcss": "^3.4.0"
  }
}
